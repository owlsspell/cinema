import { Button, Input } from "@mui/material"

const Login = () => {
    return (
        <div>
            <Input/>
            <Input/>
            <Button>Send</Button>
        </div>
    )
}

export default Login